# ============================================================================
# n8n Environment Variables
# Kopiere diese Datei zu .env und fülle die Secrets aus
# NIEMALS .env in Git committen!
# ============================================================================

# === Database ===
DB_POSTGRESDB_DATABASE=n8n
DB_POSTGRESDB_USER=n8n
DB_POSTGRESDB_PASSWORD=CHANGE_ME_SECURE_DB_PASSWORD

# === n8n Security ===
# Generiere mit: [System.Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
N8N_ENCRYPTION_KEY=CHANGE_ME_32_BYTE_BASE64_KEY

# Basic Auth für zusätzlichen Schutz (neben Cloudflare Access)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# === Public Hostname ===
N8N_HOST=ai.philogichub.com

# === Webhook HMAC Secret ===
# Wird für Signierung von PhilogicAI → n8n Webhooks verwendet
# Generiere mit: [System.Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
N8N_WEBHOOK_HMAC_SECRET=CHANGE_ME_32_BYTE_BASE64_HMAC_SECRET

# === Hardening (optional anpassen) ===
N8N_PUBLIC_API_DISABLED=true
N8N_PUBLIC_API_SWAGGERUI_DISABLED=true
N8N_PAYLOAD_SIZE_MAX=16
N8N_FORMDATA_FILE_SIZE_MAX=200

# === Monitoring (optional) ===
N8N_METRICS=false

# ============================================================================
# WICHTIG: Nach dem Ausfüllen:
# 1. Sichere .env an sicherem Ort (außerhalb Git)
# 2. Setze Dateirechte: icacls .env /inheritance:r /grant:r "%USERNAME%:F"
# 3. Backup N8N_ENCRYPTION_KEY separat (ohne Key = Datenverlust!)
# ============================================================================
